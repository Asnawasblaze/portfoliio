export interface NavItem {
  label: string;
  href: string;
}

export interface KeeperCharacter {
  id: string;
  name: string;
  role: string;
  imageUrl: string;
  color: string;
}

export interface FactionData {
  title: string;
  description: string;
  imageUrl: string;
}